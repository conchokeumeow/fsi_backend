# FSI Academic System - Environment Configuration Template
# Copy this file to '.env' and update with your actual values

# ===== PROJECT CONFIGURATION =====
PROJECT_NAME=FSI Academic System
ENVIRONMENT=local
# Options: local, staging, production

# ===== DATABASE CONFIGURATION =====
# IMPORTANT: For Supabase, use Session Pooler (IPv4 compatible), NOT Direct Connection!
# Get connection details from: Supabase Dashboard → Connect → Session pooler

# Supabase Session Pooler Configuration (Recommended)
POSTGRES_SERVER=aws-1-[REGION].pooler.supabase.com
# Example: aws-1-ap-northeast-2.pooler.supabase.com

POSTGRES_PORT=5432
# Session Pooler uses port 5432 (Transaction mode uses 6543)

POSTGRES_USER=postgres.[YOUR-PROJECT-REF]
# Example: postgres.sbpuadmzomzwvkfroisd
# Note: Username format is postgres.PROJECT-REF (with dot separator)

POSTGRES_PASSWORD=[YOUR-DATABASE-PASSWORD]
# Get from Supabase Dashboard → Settings → Database
# If forgotten, you can reset it there

POSTGRES_DB=postgres
# Default database name is 'postgres'

# ===== ALTERNATIVE: Local PostgreSQL =====
# For local development with PostgreSQL:
# POSTGRES_SERVER=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=fsi_user
# POSTGRES_PASSWORD=your_local_password
# POSTGRES_DB=fsi_academic_db

# ===== SECURITY CONFIGURATION =====
# Generate a secure random key with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING_MIN_32_CHARS
ACCESS_TOKEN_EXPIRE_MINUTES=11520
# 11520 minutes = 8 days

# ===== CORS CONFIGURATION =====
# Frontend URL
FRONTEND_HOST=http://localhost:3000

# Allowed CORS origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173
# For production, add your actual frontend domain

# ===== FIRST SUPERUSER =====
# Admin account that will be created on first initialization
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=CHANGE_THIS_PASSWORD
FIRST_SUPERUSER_NAME=System Administrator

# ===== MONITORING (Optional) =====
# Sentry DSN for error tracking
# Get from: https://sentry.io
SENTRY_DSN=

# ===== SUPABASE CONNECTION NOTES =====
# 1. Use Session Pooler for IPv4 compatibility (aws-X-region.pooler.supabase.com)
# 2. DO NOT use Direct Connection (db.PROJECT-REF.supabase.co) - it's IPv6 only
# 3. Username format: postgres.PROJECT-REF (example: postgres.abcdefghijklmnop)
# 4. Port 5432 for Session mode, 6543 for Transaction mode
# 5. Get all details from: Supabase Dashboard → Connect button → Session pooler tab

# ===== GENERAL NOTES =====
# 1. NEVER commit the actual .env file to Git!
# 2. Always use strong passwords in production
# 3. Generate a new SECRET_KEY for each environment
# 4. Update BACKEND_CORS_ORIGINS with your actual frontend URL
# 5. Email features are disabled - admin manages user credentials manually
